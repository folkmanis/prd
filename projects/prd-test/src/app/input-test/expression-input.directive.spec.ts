import { ExpressionInputDirective } from './expression-input.directive';

describe('ExpressionInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpressionInputDirective();
    expect(directive).toBeTruthy();
  });
});
